x1 <- rnorm(50) # ???????????????? ?????????????????? ?????????????? ???? 50 ??????????????????
x2 <- rnorm(50) # ???????????????? ?????????????????? ?????????????? ???? 50 ??????????????????
y <- rnorm(50) # ???????????????? ?????????????????? ?????????????? ???? 50 ??????????????????
na_index <- sample(1:30, runif(1,5,15)) # ???????????????? ?????????????????????? ???????????????????? ?? ?????????????? NA ?? ?????????????? y
y[na_index] <- NA # ???????????????? NA ?? ?????????????? y
my_df <- data.frame(x_1 = x1, x_2 = x2, y = y )
  
#new_data <- 
#fit <- lm(y ~ x1 + x2, data = my_df)
#summary(df)
#df$
#my_df$y_fill <- predict(fit,newdata = my_df)
my_df$y_full <- predict(lm(y ~ x_1 + x_2, data = my_df),newdata = my_df)

#for (i in 1:length(my_df)){
#  print(my_df[3])
  
}